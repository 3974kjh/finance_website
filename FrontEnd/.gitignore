# Frontend specific overrides (if needed)
# Most rules are handled by the root .gitignore

# Local environment files specific to this frontend project
.env.frontend
.env.frontend.*

.DS_Store
node_modules
/build
/.svelte-kit
/package
.env
.env.*
!.env.example
.vercel
.output
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
